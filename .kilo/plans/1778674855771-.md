# Plan: Clean Ask AI Search Interface

## Goal
Simplify the Ask AI modal to show just a clean search bar with explainer text, removing all the example suggestion buttons.

## Current State
The AI Assistant modal currently has:
- Title "Ask AI"
- Intro text about natural language search
- 4 example buttons (Events tomorrow, Events at Tata Theatre, Missing requirements, Ashwin's events)
- Search input with placeholder
- Send button
- Response display area

## Proposed Changes

### HTML Template Changes (`src/index.tsx` lines 2982-2996)
1. Replace the 4 example buttons with a simple explainer paragraph
2. Keep the search input and send button
3. Maintain the response display area

### New Structure
```
<div class="mb-6">
  <p class="text-gray-600 mb-4">Search your events using natural language. Ask anything about dates, venues, crew, or availability.</p>
  
  <p class="text-gray-500 text-sm mb-4">
    Examples: "Events at JBT tomorrow", "When is crew free in December?", "Shows with missing sound requirements"
  </p>
  
  <div class="flex space-x-2">
    <input type="text" id="aiQueryInput" placeholder="Ask about your events..." 
             class="flex-1 px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#98A2D7]"
             onkeypress="if(event.key==='Enter') askAI()">
    <button onclick="askAI()" class="btn-primary px-6 py-3">
      <i class="fas fa-paper-plane"></i>
    </button>
  </div>
</div>
```

## Files to Modify
- `src/index.tsx` - The AI Assistant modal HTML template

## Implementation Steps
1. Edit the AI Assistant modal HTML to remove example buttons
2. Add simple text-based examples in the description
3. Test the build
4. Deploy